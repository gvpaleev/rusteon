// Задание. Называем многоугольники
// Многоугольник – плоская геометрическая фигура с тремя или более сторонами, пересекающимися в трех или более точках (вершинах).
//
// Дается число, которое означает количество сторон фигуры, вам нужно распечатать название фигуры. Например,
// дается число 5 - программа печатает слово пятиугольник.
//
// Примечание: Много фигур не нужно обрабатывать, для остальных можете придумать какую-нибудь хитрость.

use std::io::{self, Write};

fn main() {
    print!("Введите количество сторон: ");
    io::stdout().flush().unwrap(); // чтобы prompt появился сразу

    let mut input = String::new();
    io::stdin().read_line(&mut input).expect("Ошибка ввода");

    let sides: u32 = match input.trim().parse() {
        Ok(num) => num,
        Err(_) => {
            println!("Нужно ввести целое положительное число!");
            return;
        }
    };

    let name = match sides {
        3 => "треугольник",
        4 => "четырёхугольник",
        5 => "пятиугольник",
        6 => "шестиугольник",
        7 => "семиугольник",
        8 => "восьмиугольник",
        _ if sides >= 9 => "какой-то сложный многогранник 🧐",
        _ => "это не многоугольник 🤔",
    };

    println!("Это — {}", name);
}
